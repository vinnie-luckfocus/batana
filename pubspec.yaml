name: batana
description: 棒球挥棒动作分析系统 - MVP 阶段
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  camera: ^0.11.1
  sqflite: ^2.3.0
  path_provider: ^2.1.1
  go_router: ^13.0.0
  mediapipe_pose: ^0.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
